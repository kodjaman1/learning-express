<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer">

    <% if (!locals.chosen) { %>
     
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else {%>

      <h2 id="recipteTitle"> Recipe name is <%= chosen.name %> </h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li> <%= chosen.ingredients.protein.name %>, <%= chosen.ingredients.protein.preparation %></li>
        <li> <%= chosen.ingredients.salsa.name %> </li>
        <li> <% chosen.ingredients.toppings.forEach(element => { %>
          <li>
            <%= element.quantity %> of <%= element.name %>
          </li>  
          <% }) %>
      </ul>

      <% } %>



  </div>
</body>

</html>